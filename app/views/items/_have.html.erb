<%= 
=begin
link_to "" , ownerships_path(item_id: item.id , type: "Have") ,
                      :class => "have-#{item.id} btn-have btn btn-warning btn-sm have",
                      :remote => true,
                      :method => :post 
=end
%>

          <%=
=begin
link_to "" , ownerships_path(item_code: item['itemCode'] , type: "Have") ,
                      :class => "have-#{item.id} btn-have btn btn-warning btn-sm have",
                      :remote => true,
                      :method => :post
=end
%>
<%= link_to "" , ownerships_path(item_id: item.id , type: "Have") ,
                      :class => "have-#{item.id} btn-have btn btn-warning btn-sm have",
                      :item_code => "have-#{item.id}",
                      :remote => true,
                      :method => :post %>